# GitHub

- コミットメッセージ、PRタイトル・説明、issueコメントなど、GitHubに関するテキストは基本的に日本語で書く
- `git push` したら必ず `gh run watch` でCIの結果を確認し、失敗していたらその場で原因を調べて修正する
- 変更はブランチを切ってPRで進める（mainへの直接pushは禁止）
- CIが通ってコンフリクトがなければそのままマージしてOK
